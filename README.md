# zynq-sdr-lab

`zynq-sdr-lab` is a minimal, step-by-step SDR learning project based on **Xilinx Zynq**.

The project starts from a **hardware DDS**, and gradually builds toward a complete SDR baseband system.
